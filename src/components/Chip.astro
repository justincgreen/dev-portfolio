---
const {title, color, hex} = Astro.props;
---

<div class="c-chip">
  <div class={`${color !== undefined ? `c-chip__single c-chip__single--${color}` : "c-chip__single c-chip__single--no-color"}`}></div>
  <div class="c-chip__right">
    <p class="c-chip__single--title">{title}</p>
    <div class="c-chip__single--hex">{hex}</div>
  </div>
</div>

<!-- <script define:vars={{color}}>
  const getColor = () => {
    if(color === "green--dark") {
      console.log(color);
    }
  }
 getColor(); 
</script> -->

<style lang="scss">
// Move this to individual component scss file later - file mixed with scss gets too messy in this component file
.c-chip {
  display: inline-flex;
  margin: 0 1em 1em 0;
  align-items: center;  
  
  &__single {
    width: 50px;
    height: 50px;
    margin: 0 auto;
    border-radius: 50%;
    border: 3px solid var(--c-white);
    
    &--title {
      margin-left: 10px;
    }
    
    &--hex {
      margin-left: 10px;
    }
    
    &--no-color {
      background-image: linear-gradient(45deg, #808080 25%, transparent 25%), linear-gradient(-45deg, #808080 25%, transparent 25%), linear-gradient(45deg, transparent 75%, #808080 75%), linear-gradient(-45deg, transparent 75%, #808080 75%);
      background-size: 20px 20px;
      background-position: 0 0, 0 10px, 10px -10px, -10px 0px;
    }
    
    &--green {
      background-color: var(--c-green);
      
      &--dark {
        background-color: var(--c-green--dark);
      }
      
      &--darkest {
        background-color: var(--c-green--darkest);
      }
    }
    
    &--navy {
      background-color: var(--c-navy);
    }  
    
    &--slate {
      background-color: var(--c-slate);
      
      &--light {
        background-color: var(--c-slate--light);
      }
      
      &--lightest {
        background-color: var(--c-slate--lightest);
      }
    }    
    
    &--white {
      background-color: var(--c-white);
    }    
  }  
}
</style>